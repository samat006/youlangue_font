defaultConfig {
    // Ton nouvel identifiant unique
    applicationId "com.touba7g.voxtube" 
    
    // Minimum requis pour les lecteurs vidéo modernes
    minSdkVersion 21 
    targetSdkVersion 34
    
    versionCode flutterVersionCode.toInteger()
    versionName flutterVersionName
}